# YASOOO — سجل التطوير الشامل (Project Development Log)

> **الغرض من هذا الملف:** توثيق كامل لكل ما تم إنجازه في نظام YASOOO لتحليل التزلج الفني،
> بصيغة يسهل على أي مساعد ذكاء اصطناعي قراءتها والمتابعة من حيث توقف العمل — دون الحاجة
> لإعادة قراءة كامل سجل المحادثات السابقة.
>
> **آخر تحديث:** 2026-09-11 · **الفرع النشط:** `claude/continue-work-KRPKT`
> **المستودع:** `elmansoory/yasooo` على GitHub

---

## 1. نظرة عامة على المشروع

**YASOOO** نظام شامل لتحليل أداء لاعبي التزلج الفني، مبني بالكامل على **Python + Streamlit**
(لا يوجد React/Next.js أو أي بنية Node.js في المشروع). يجمع بين:

- تحليل فيديو بالذكاء الاصطناعي (MediaPipe + نماذج مخصصة) لاكتشاف القفزات والدورانات والأخطاء
- مكتبة معايير اختبارات رسمية (ISI وISU) لتقييم نجاح/رسوب اللاعب
- إدارة نادٍ كاملة (أعضاء، حضور، مدفوعات، جداول، منافسات)
- لوحات تحكم للمدربين وأولياء الأمور

### قاعدة الملكية والتعليمات الدائمة
- **"ابدا من الاول الي النهاية انت مالك البرنامج"** — ملكية كاملة للنظام، تنفيذ فوري دون انتظار موافقات متكررة على القرارات التقنية الواضحة.
- **دائماً ادفع التحديثات إلى GitHub** بعد كل تغيير — الفرع: `claude/continue-work-KRPKT`.
- **⚠️ أمان:** مفتاح API `q9l0HJV3DzhhVzwsQo8N5txFtoyDASiJe87ddVll` شاركه المستخدم في جلسة سابقة — **تم رفضه ولا يُستخدم إطلاقاً تحت أي ظرف.**
- **لا تُضاف تبعيات/مكتبات خارجية غير مرتبطة بطبيعة المشروع** (مثال: طُلب مرة إضافة مكتبة React اسمها "brainless" لواجهات وكلاء برمجية — رُفض لعدم ملاءمته لمنتج تحليل رياضي).

---

## 2. الملف الرئيسي للتشغيل

| السياق | الملف |
|---|---|
| **التطبيق الفعلي (Python)** | `final_app.py` — يحتوي كل الصفحات عبر `_ROUTER` |
| **تشغيل ويندوز** | `YASOOO.bat` ← **الملف الوحيد المعتمد للمستخدم على ويندوز** |
| **تشغيل لينكس/ماك** | `run.sh` |
| **قاعدة البيانات** | `skating_database.db` (SQLite) — تُنشأ وتُصلَح تلقائياً عبر `init_db()` في `final_app.py` |

> ⚠️ **ملفات قديمة يجب تجاهلها تماماً:** `app.py`, `advanced_app.py`, `professional_app.py`,
> وملفات `.bat` القديمة (`START.bat`, `run.bat`, `QUICK_START.bat`) — كانت تستخدم مخططاً قديماً
> لقاعدة البيانات (جدول `members`) غير متوافق مع `final_app.py` (يستخدم جدول `skaters`). تم توحيد
> كل ملفات `.bat` لتُشغّل `final_app.py` حصراً، لكن الملفات القديمة ما زالت موجودة في المستودع
> ولا يجب الاعتماد عليها أو تعديلها.

---

## 3. البنية المعمارية للكود

```
final_app.py                          # نقطة الدخول — التنقل، الترجمة، التوجيه (_ROUTER)
src/
├── pages/                            # كل صفحة Streamlit في ملف منفصل
│   ├── video_analysis_page.py        # 🧪 مختبر تحليل الفيديو (الأهم)
│   ├── test_standards_page.py        # 🏅 امتحانات ISI (+ يُعاد استخدامه لـ ISU)
│   ├── isu_exams_page.py             # 🏅 امتحانات ISU (غلاف رقيق فوق test_standards_page)
│   ├── training_data_hub_page.py     # 🤖 مركز تدريب النموذج
│   ├── fse_analysis_page.py          # 📊 تحليل FSE المالي
│   └── ... (عشرات الصفحات الأخرى: أعضاء، حضور، مدفوعات...)
├── analysis/
│   ├── ai_movement_engine.py         # يربط MovementClassifier + LSTM + PoseComparator
│   ├── coaching_knowledge.py         # قاعدة معرفة تدريب فني (أسباب جذرية + جمل تدريب)
│   └── test_standards.py             # محرك معايير الاختبار + التقييم (ISI/ISU)
├── models/                           # MovementClassifier, LSTMClassifier, إلخ (كانت موجودة مسبقاً)
└── utils/
    └── ysoo_theme.py                 # نظام تصميم موحّد (CSS) — خطوط Cairo/Tajawal، بطاقات، KPI
```

---

## 4. ملخص العمل المُنجز (بالترتيب الزمني التقريبي)

### 4.1 ربط محرك الذكاء الاصطناعي بخط تحليل الفيديو
- **الملف:** `src/analysis/ai_movement_engine.py` (جديد)
- كانت ثلاث وحدات ذكاء اصطناعي موجودة لكن غير مربوطة فعلياً: `MovementClassifier`،
  `LSTMClassifier`، `PoseComparator`.
- الآن `AIMovementEngine.enhance_jump()` / `enhance_spin()` / `detect_step_sequences()` تُستدعى
  داخل `SkatingVideoAnalyzer.analyze()` في `video_analysis_page.py` بعد اكتشاف القفزات/الدورانات
  الأولي، وتضيف: `ai_code`, `ai_confidence`, `ai_edge`, `ai_takeoff`, `pose_score`,
  `pose_corrections_ar`, `lstm_label`.

### 4.2 قاعدة معرفة التدريب الفني (Coaching Knowledge Base)
- **الملف:** `src/analysis/coaching_knowledge.py` (جديد)
- مبنية على محتوى مرجعي حقيقي رفعه المستخدم (سلسلة "Figure Skating Coach Ready" — Bonus 2
  مكتبة جمل التصحيح، Module 3 مكتبة تقنيات القفزات/الدورانات، Bonus 3 برامج الإعداد البدني).
- لكل خطأ مكتشف: **السبب الجذري الحقيقي** (بمبدأ "المرحلة السابقة" — الخطأ الظاهر جذره غالباً في
  المرحلة التي تسبقه، وليس في مكان ظهوره)، **3-5 جمل تدريب فورية جاهزة**، **تمرين تصحيح معزول**،
  **تمرين بدني مكمّل**. يظهر في تبويب "⚠️ الأخطاء والتصحيحات" ضمن مختبر تحليل الفيديو.

### 4.3 تصميم الهيكل العظمي — تحليل فيديو مرجعي
- حُلّل فيديو مرجعي (مقارنة 2026 مقابل أولمبياد بكين 2022) لتحديد البرنامج المستخدم: **MediaPipe
  Pose** بترميز ألوان لكل جزء جسدي.
- طُبِّق نفس النظام اللوني بالضبط في `_draw_skeleton_on_frame()` و`_build_3d_skeleton_fig()`:
  🟣 الوجه = وردي · ⚪ الجذع = أبيض · 🟡 الذراع اليسرى = كهرماني · 🔵 الذراع اليمنى = أزرق ·
  🟢 الساق اليسرى = أخضر · 🩵 الساق اليمنى = تيل.

### 4.4 نظام التصميم الموحّد (Design System)
- **الملف:** `src/utils/ysoo_theme.py` (جديد)
- مستوحى من تحليل واجهة React مرجعية (shadcn/ui) رفعها المستخدم — استُخرجت لغة التصميم فقط
  (لا يمكن تشغيل React داخل Streamlit): خطوط Cairo/Tajawal، بطاقات أخطاء ملوّنة بالحدة (3 مستويات:
  حرج/كبير/بسيط)، صفوف KPI بأيقونات.
- مُطبَّق في `video_analysis_page.py` (تبويبي النتائج والأخطاء).

### 4.5 "مختبر تحليل الفيديو" — إعادة توجيه المنصة بالكامل
بناءً على طلب صريح: "حوّل المنصة إلى مختبر تحليل فيديو... مع توضيح مستوى الثقة وحدود اللقطة
بدلاً من تقديم تخمينات قطعية":
- **فيديو هيكل عظمي متحرك حقيقي** (وليس صوراً ثابتة فقط) — `_extract_poses_mediapipe` يبني الآن
  فيديو overlay كامل عبر `cv2.VideoWriter` (بحد أقصى 120 ثانية، بتصغير الدقة لـ640px)، يُعرض
  عبر `st.video()` في تبويب "🦴 الهيكل العظمي المتحرك".
- **لوحة "دقة التحليل وحدود اللقطة" دائمة الظهور** — تعرض دائماً (وليس فقط عند ضعف الثقة): نسبة
  تغطية الهيكل العظمي، عدد الإطارات المحلَّلة، المحرك المستخدم (MediaPipe الحقيقي أم احتياطي).
- **لغة احتمالية**: "الحركة المرجّحة" بدل التأكيد المطلق، مع شارة ثقة % على كل بطاقة قفزة.
- إعادة تسمية العنوان: **"🧪 مختبر تحليل الفيديو"**.

### 4.6 إصلاح الإيجابيات الكاذبة في اكتشاف القفزات/الدورانات
مشكلتان جذريتان اكتُشفتا وأُصلحتا في `video_analysis_page.py`:
1. **كاشف القفزات** كان يعتمد فقط على انخفاض إحصائي في موضع الورك، دون التحقق من مؤشر
   "في الهواء" الفعلي → أُضيف تحقق تقاطعي (`airborne_ratio >= 0.30`) + حد أدنى لجودة نقاط
   الهيكل العظمي + رفض الانخفاضات الحدّية الضعيفة.
2. **كاشف الدورانات** كان يتحقق فقط من عدم الحركة (وليس من الدوران الفعلي) → أُضيف تحقق من
   معدل التذبذب (`cycles/sec >= 0.5`).
3. **إصلاح لاحق مهم (بعد اختبار حقيقي):** عتبة `lateral_travel` الثابتة (0.025) كانت ترفض
   قفزات حقيقية من كاميرات بعيدة (حلبات مفتوحة) — أصبحت **عتبة تكيّفية** تتناسب مع مدى حركة
   اللاعب الفعلي في كل فيديو: `min(0.025, max(0.006, global_x_std * 0.06))`.
- ✅ **مؤكَّد بتشغيل حقيقي**: على فيديوهين حقيقيين من حلبة SunCity، انتقل الكشف من 0 قفزات (خطأ)
  إلى قفزة واحدة صحيحة في كل فيديو (مطابقة للمراجعة البصرية اليدوية)، بدون عودة أي دورانات وهمية.

### 4.7 إصلاحات تشغيل ويندوز (YASOOO.bat) — سلسلة إصلاحات متتالية
1. توحيد كل ملفات `.bat` لتشغيل `final_app.py` بدل `app.py` القديم (سبب خطأ
   `no such table: members`).
2. تثبيت Python تلقائياً عند غيابه (`winget` ثم تنزيل مباشر كبديل).
3. **مرونة اكتشاف Python** — لا يشترط إصدار 3.11 بالتحديد، يجرّب `py -3.11` ثم `py -3` ثم `py`
   ثم `python`.
4. **تحديث PATH بطريقة موثوقة** — استبدال `reg query` (يُرجع قيماً غير مفكوكة) بقراءة عبر
   PowerShell/.NET `GetEnvironmentVariable`، + بحث مباشر في مجلدات التثبيت الشائعة كحل أخير.
5. **تصحيح إصدار protobuf تلقائياً** — protobuf 5.x يكسر MediaPipe
   (`'MessageFactory' object has no attribute 'GetPrototype'`) → تثبيت `protobuf>=3.20,<5` دائماً.
6. **تنزيل نموذج MediaPipe تلقائياً** — اكتُشف أن `data/models/pose_landmarker_lite.task`
   (~6MB) غير موجود في المستودع ولا يُنزَّله أي سكريبت، ما يعني سقوطاً صامتاً لطريقة احتياطية
   أقل دقة بكثير. أُضيف تنزيل تلقائي عند أول تشغيل.

### 4.8 إصلاح NameError في محرر الموسيقى
- **الملف:** `src/utils/audio_processor.py`
- `numpy` كان مستورداً فقط داخل `try: import librosa` — إذا فشل `librosa`، ينهار التطبيق عند
  تحميل `music_editor_page` بسبب `NameError: name 'np' is not defined` في تلميح نوع الإرجاع
  `-> np.ndarray`. أصبح `numpy` استيراداً غير مشروط.

### 4.9 مكتبة معايير الاختبار (Test Standards Lab) — ISI ثم ISU
بناءً على طلب: "برمج برنامج... اني ساعطيك كل المعطيات... يستطيع نرى إذا كان اللاعب ناجح أم لا":

- **`src/analysis/test_standards.py`** — محرك كامل:
  - جدول `test_standards` (SQLite) — كل معيار له `key`, `name_ar/en`, `source_note`,
    `elements_json`, `federation` (ISI/ISU).
  - `evaluate(results, standard, manual_overrides)` — يقارن كل عنصر مطلوب بالقفزات/الدورانات/
    تسلسلات الخطوات المكتشفة فعلياً، ويُرجع حكماً متدرجاً: `pass` / `warn` (اكتُشف لكن دون
    المستوى) / `fail` (لم يُكتشف) / `manual` (لا كاشف آلي له — العناصر غير الدورانية مثل
    Arabesque أو Three-Turns تُترك صراحة للمدرب، لا تُخمَّن).
  - جدول `standard_evaluations` — كل تقييم يُحفظ تلقائياً (لاعب، معيار، نتيجة، تاريخ).
  - جدول `reference_video_uploads` — لحفظ الفيديوهات المرجعية المرفوعة بانتظار استخراج معاييرها.
  - `export_training_dataset()` — يُصدِّر كل شيء (معايير + تقييمات + فيديوهات مرجعية) كـ JSON.

- **معايير ISI المزروعة (6، مستخرجة من فيديوهات ISI الرسمية المرفوعة):**
  | المستوى | عدد العناصر | ملاحظة |
  |---|---|---|
  | Pre-Alpha | 5 | Two-Foot Glide, One-Foot Glide, Forward/Backward Swizzles, Backward Wiggle |
  | Alpha | 3 | Forward Stroking, Forward Crossovers, One Foot Snowplow Stop |
  | Beta | 3 | Backward Stroking, Backward Crossovers, T-Stops |
  | Gamma | 3 | Forward Outside Three Turns, Inside Open Mohawk Combinations, Hockey Stop |
  | Delta | 4 | Forward Inside Three Turns, Forward Edges, Shoot-the-Duck/Lunge, Bunny Hop |
  | Freestyle 4 | 6 | Flip Jump, Loop Jump, Sit Spin, Half Loop Jump, Two Backward Arabesques, Dance Step Sequence |

  > كل عناصر Pre-Alpha→Delta مُصنَّفة `CATEGORY_POSITION` (تتطلب مراجعة يدوية من المدرب) لأنها
  > مهارات حواف/دفع/إيقاف بدون دوران — لا يوجد لها كاشف آلي. عناصر Freestyle 4 القفزية/الدورانية
  > تُقيَّم آلياً.

- **معيار ISU واحد مزروع** (`isu_jump_basics`): Waltz Jump, Toe Loop, Salchow — مبني على بيانات
  حقيقية من Module 3 (Jump & Spin Technique Library)، وله كشف آلي حقيقي (هذه قفزات دورانية
  يكشفها المحرك).

- **الصفحات:**
  - `src/pages/test_standards_page.py` — `show_federation_exams_page(federation)` بأربع تبويبات:
    📤 رفع فيديو مرجعي معتمد · 🎯 تقييم لاعب · 📚 المكتبة المرجعية · ➕ إضافة معيار يدوياً.
  - `src/pages/isu_exams_page.py` — غلاف رقيق يستدعي نفس الدالة بـ `federation='ISU'`.
  - `src/pages/training_data_hub_page.py` — "🤖 مركز تدريب النموذج": إحصائيات مجمّعة من كلا
    الاتحادين، سجل تقييمات قابل للتصفح، تصدير JSON. **يوضّح صراحة أنه لا يُعيد تدريب النماذج
    فعلياً** (ذلك من صفحة "🤖 تدريب النموذج" الأصلية) — فقط يُجهّز البيانات.

- **تحسين لاحق مهم:** رفع فيديو اللاعب أصبح ممكناً **مباشرة داخل تبويب "🎯 تقييم لاعب"** (دالة
  `_inline_player_upload`) — لا حاجة للتنقل بين صفحتين. كان هذا مصدر ارتباك حقيقي (تبويب "رفع
  فيديو مرجعي" مخصص للفيديو الرسمي المعتمد، وليس فيديو اللاعب).

### 4.10 التحقق الشامل بتشغيل حقيقي (End-to-End Validation)
- ثُبِّتت بيئة MediaPipe/OpenCV حقيقية في بيئة الاختبار (حُلَّت مشاكل مكتبات `libEGL`/`libGLESv2`
  الناقصة)، ونُزِّل نموذج `pose_landmarker_lite.task` الفعلي.
- شُغِّل `SkatingVideoAnalyzer.analyze()` الحقيقي على فيديوهين حقيقيين من حلبة SunCity — أدى هذا
  لاكتشاف واختبار وإصلاح خلل عتبة `lateral_travel` الموصوف في §4.6.
- شُغِّل `test_standards.evaluate()` على نتائج التحليل الحقيقية ضد معياري Freestyle 4 وDelta —
  تأكَّد أن النظام **لا يختلق نتائج**: يُظهر بصدق "❌ NOT YET" عندما لا تُكتشف عناصر كافية،
  بدل تزوير نجاح وهمي.

### 4.11 حملة تدقيق شاملة على دقة الكشف — "حسّن دقة كذا بنفس الطريقة"
طلب المستخدم تكرار منهجية §4.6 (تشخيص على بيانات حقيقية → إصلاح مُتحقَّق منه → إعادة اختبار
شاملة) على بقية أجزاء محرك التحليل. أربعة إصلاحات متتالية، كلها موثّقة بأرقام حقيقية من فيديوهي
SunCity:

1. **كاشف الدورانات كان لا يكتشف أي دوران إطلاقاً — نفس فئة خلل القفزات لكن أعمق.**
   الاختبار الثابت "لا يتحرك كثيراً خلال ثانية" كان يُحقَّق في 1388 من أصل 1389 إطار (الفيديو كله
   تقريباً!) لأن الحركة لكل ثانية طبيعياً صغيرة من كاميرا بعيدة. هذا يدمج الفيديو كله في "كتلة
   واحدة"، فيُخفي أي دوران حقيقي لأن معدل التذبذب يُحسب كمتوسط على 90+ ثانية.
   **الإصلاح (مرحلتان):**
   (أ) نوافذ ترشيح قصيرة (1.2 ثانية) تتطلب معدل تذبذب محلي مرتفع فعلاً (`cycles/sec >= 0.8`) بدل
   الاعتماد فقط على "عدم الحركة" — تعزل مقاطع قصيرة بدل كتلة ضخمة واحدة.
   (ب) **تأكيد بالدوران الحقيقي**: لكل مقطع مرشَّح، يُقاس الدوران الفعلي عبر زاوية خط الكتفين
   (نفس تقنية قياس دوران القفزات، `_compute_rotation_from_kp`) ويُشترط ≥2.5 دورة كاملة — هذا هو
   الفيصل الحقيقي بين الدوران والالتفاف العادي: تمايل النهوض من الركوع سجّل 0.0 دورة، لحظات
   الالتفاف العادي سجّلت ~1.5-2.4 دورة، والدوران الحقيقي المؤكَّد بصرياً سجّل 8.8 دورة.
   ✅ **النتيجة:** SunCity_1: 0→2 دوران (6.0 و4.2 دورة) · SunCity_2: 0→1 دوران (8.8 دورة، مطابق
   تماماً لدوران تحقّقتُ منه بصرياً في الفيديو). القفزات بقيت دون تراجع.

2. **فحص جودة الوضعية بالذكاء الاصطناعي كان "كوداً ميتاً" — خطأ في مقياس الأرقام (0-100 مقابل 0-1).**
   `_detect_ai_errors` كان يقارن `pose_score` بعتبة `0.55`، لكن `PoseComparator._score_from_deviations()`
   يُرجع الدرجة على مقياس **[0, 100]** فعلياً. تأكَّد هذا مباشرة: قفزة قيّمها النظام نفسه "مقبول
   ⭐⭐" سجّلت `pose_score=57.6` — رقم أكبر بكثير من 0.55، فلم يكن هذا الفحص قابلاً للتفعيل
   إطلاقاً بغض النظر عن مدى سوء الوضعية. نفس الخطأ تكرر في موضعين بالواجهة (عرض نسبة الجودة
   للقفزات والدورانات) كانا سيعرضان نسباً عبثية مثل "5760%" لو تفعّل الفحص أصلاً.
   **الإصلاح:** العتبة صُحِّحت إلى `55` (وتأثير GOE إلى `40`)، وعرض النسبة صُحِّح في 3 مواضع.
   ✅ **تحقّق مزدوج:** درجة اصطناعية 42.0 → تُطلق الخطأ الآن بنسبة صحيحة "42%" · درجة 57.6 (فوق
   العتبة) → صحيح ألا تُطلق خطأ · على الفيديوهين الحقيقيين: SunCity_1 اكتشف خطأً حقيقياً واحداً
   بدقة ("استقرار محور منخفض — Sit Spin: 52%") لم يكن يُكتشف من قبل.

3. **فحوصات الوضعية/الانحناء كانت تُعاقب التقنية الصحيحة كخطأ.**
   خللان مفاهيميان في `_detect_errors`:
   (أ) فحص عدم تناسق الذراعين كان يُطبَّق على التزلج **كله** رغم أن تعليقه بالكود يقول "أثناء
   الدوران فقط". خارج الدوران — أثناء الدفع والخطوات المتقاطعة — عدم تناسق الذراعين هو **التقنية
   الصحيحة** (معاييرنا ISI Alpha/Beta تشترطها حرفياً: "الذراع الخارجية أمام، الداخلية خلف"). أصبح
   الفحص مقيَّداً بنوافذ الدوران فقط.
   (ب) إحصائيات الانحناء وعدم التناسق كانت تشمل لحظات القفزات والدورانات، حيث الميل قد يكون جزءاً
   مطلوباً من الوضعية (دوران Camel/Layback). استبعادها غيّر نسبة الانحناء من 12.6% إلى 14.0% على
   بيانات حقيقية — تلوّث حقيقي وإن كان بسيطاً.
   **محاولة رُفضت بصدق:** جُرِّب حساب الانحناء بزاوية ثلاثية الأبعاد حقيقية باستخدام إحداثي العمق
   Z (بدل الاعتماد على المستوى ثنائي الأبعاد فقط الذي لا يميّز الانحناء الحقيقي عن الميل الجانبي
   الطبيعي أثناء الالتفاف). لكن هذا رفع نسبة الانحناء المكتشف من 12.6% المعقولة إلى **60.3% غير
   معقولة إطلاقاً** — تقديرات العمق في MediaPipe من كاميرا واحدة غير موثوقة بما يكفي لهذا الغرض.
   **لم يُطبَّق هذا التغيير** — مسجَّل هنا حتى لا يُعاد تجربته دون حل مشكلة الضجيج أولاً.
   ✅ **تحقّق:** نتائج الفيديوهين الحقيقيين مطابقة تماماً قبل وبعد (لا تراجع)، مع كود أكثر سلامة
   مفاهيمياً لفيديوهات أخرى بها حركات دفع/تقاطع أكثر.

**الدرس العام المتكرر أربع مرات في هذه الحملة:** أي حساب هندسي (زاوية، مسافة، عتبة) مبني على
افتراض ضمني عن حجم اللاعب في الإطار أو عن نوع العنصر الجاري تنفيذه — سيفشل بصمت على فيديوهات لا
تطابق ذلك الافتراض. الإصلاح الحقيقي دائماً يحتاج قياساً مُقاوِماً لهذه المتغيرات (مثل الدوران
الفعلي بدل التذبذب، أو الفحص المقيَّد بنوع العنصر) — وليس مجرد تعديل رقم العتبة.

---

### 4.12 استبدال معايير ISI بالنص الرسمي الكامل + إصلاح المشغّل + تصحيح جذري لخطأ numpy

المستخدم لاحظ أن تبويب "تقييم لاعب" قارن فيديو Pre-Alpha بمعيار Freestyle 4 خطأً (اختيار يدوي)
وأنتج نتيجة مضلِّلة. هذا فتح سلسلة عمل كبيرة:

**أ) `suggest_standard()` — اقتراح المعيار المناسب تلقائياً** (`test_standards.py`)
يقارن ما اكتُشف فعلياً (عدد القفزات/الدورانات) بمتطلبات كل معيار، ويُختار تلقائياً في القائمة
المنسدلة + تحذير صريح إن اختار المدرب معياراً لا يناسب المحتوى المكتشف. عند عدم وجود أي دوران،
يفترض أبسط مستوى (Pre-Alpha) لا أي معيار عشوائي.

**ب) استبدال معايير ISI الكاملة بالنص الرسمي** — المستخدم رفع كتاب "The Ice Sports Industry (ISI)
Handbook" (2018 Edition) عبر Google Drive (43MB، ممسوح ضوئياً بدون طبقة نص، تعذّر تحميله كاملاً
عبر أدوات Drive بسبب حد 10MB) ثم رفع الصفحات 28-59 كملف PDF أصغر مباشرة لهذه الجلسة — قُرئت
بـ OCR (33 صفحة) واستُخرج منها **15 معياراً رسمياً كاملاً** (Pre-Alpha → Delta + Freestyle 1-10)
تحل محل البيانات المستخرجة من الفيديوهات سابقاً (كانت أقل دقة وتغطي 6 مستويات فقط).
`ensure_seed_standards()` أصبحت تُحدِّث (`UPDATE`) الصفوف المزروعة الموجودة بدل تجاهلها إن كان
المفتاح موجوداً بالفعل — مهم لتوصيل التصحيح لأي قاعدة بيانات مزروعة بالنسخة القديمة.

**ج) توسيع معايير ISU لنفس عمق ISI** — كان `ISU_JUMP_BASICS` يغطي 3 قفزات فقط (Waltz, Toe Loop,
Salchow). استُخرج Loop وFlip وLutz وAxel من نفس المصدر الموثوق المُستخدم سابقاً (Module 3 — Jump
& Spin Technique Library، Figure Skating Coach Ready™، صفحات 11-44، عبر Google Drive) وأُضيفت
بمعايير دخول/إقلاع/هبوط حقيقية. اكتُشف خطأ بيانات حقيقي أثناء ذلك: `waltz_jump` كان يحمل الرمزين
`1A`/`axel` (رمز Axel الفعلي) ضمن `match_codes`، فكانت أي قفزة Axel "تنجح" في معيار Waltz Jump
بحد 0.5 دورة بدل تقييمها بمعيارها الصحيح (1.5 دورة) — فُصلا لعنصرين مستقلين. أُضيف معيار جديد
`ISU_SPIN_BASICS` (Upright/Scratch, Sit, Camel, Layback بكاشف آلي؛ Combination وChange-of-Foot
تتطلب مراجعة يدوية لأن كاشفنا الحالي يصنّف الدوران بمعدل واحد وليس تسلسل وضعيات متعددة).

**د) إصلاح `YASOOO.bat` — البرنامج لم يكن يفتح إطلاقاً على جهاز المستخدم.** استغرق التشخيص عدة
جولات فاشلة (تفصيلها مهم لتفادي تكرارها):
   1. أول رسالة خطأ ("`'X' is not recognized`" متسلسلة) شُخِّصت كقوسين غير محميين داخل `echo`
      ضمن كتلة `if (...)` متداخلة — `cmd.exe` يُغلق الكتلة عند أول `)` غير محمي حتى لو بدا
      "متوازناً" محلياً مع `(` في نفس السطر (القاعدة غير متماثلة: `(` لا يُحتسب إلا بعد كلمة
      كتلة مثل `if/for/else`، لكن أي `)` يُحتسب دائماً). أُصلح بإضافة `^` قبل كل قوس نصّي.
   2. المستخدم استمر بنفس الخطأ — تبيّن أنه كان يُشغّل نسخة قديمة (حمّل الفرع الافتراضي `main`
      بدل `claude/continue-work-KRPKT` عبر زر GitHub "Download ZIP" العام).
   3. بعد تحميل الفرع الصحيح، ظهر خطأ **أعمق**: رمز مشوّه `�` وحتى استدعاء `python` بوسيط عشوائي
      غريب — توقيع كلاسيكي لسوء قراءة الترميز، وليس مجرد قوس. السبب الجذري: الملف يحتوي نصاً
      عربياً وشرطات طويلة (—) بترميز UTF-8 بدون BOM، ويعتمد على `chcp 65001` في منتصف الملف —
      توليفة معروفة بعدم الاستقرار على بعض إصدارات/لغات ويندوز. **الحل الحاسم:** حُذف كل محتوى
      غير-ASCII من `YASOOO.bat` نهائياً (الشرطات الطويلة → عادية، حُذف السطر العربي الوحيد في
      شعار الترحيب — التطبيق الفعلي يعرض العربي بلا مشاكل، كان هذا فقط في شاشة الترحيب بالطرفية).
      **الدرس:** ملف تشغيل حرج (launcher) يجب أن يكون ASCII بحتاً دائماً — لا تُدخِل Unicode فيه
      مهما بدا آمناً، `cmd.exe` غير موثوق بما يكفي عبر كل إصدارات/لغات ويندوز.

**هـ) تصحيح جذري لخطأ `numpy.bool_`/`numpy.float64` في JSON** — بعد إصلاح المشغّل، ظهر خطأ جديد
عند أول محاولة حفظ تقييم حقيقي: `TypeError: Object of type bool_ is not JSON serializable`.
التشخيص الأول عالج فقط نقطة الاستهلاك (`evaluate()` في `test_standards.py`)، لكن تتبّع الجذر
الحقيقي كشف أن `_compute_rotation_from_kp()` (video_analysis_page.py) يُرجع `numpy.float64` من
داخليات `np.unwrap`/`np.array` — يتسرّب هذا لحقل `rotations` في **كل** قفزة ودوران. نفس النمط
تكرر في `t_start`/`t_end` (مبنية من `times[idx]` حيث `times` مصفوفة numpy)، ومدة تسلسل الخطوات
في `ai_movement_engine.py`، ودرجة `pose_score` في `pose_comparator.py`. **القاعدة العامة
المكتشفة:** `round()` أو العمليات الحسابية على عنصر مصفوفة numpy تبقى numpy-typed حتى لو بدت
كرقم عادي — يجب `float(...)`/`bool(...)` صريحة عند كل نقطة خروج من numpy لبنية بيانات ستُحفظ
كـ JSON. أُصلحت كل نقاط التسرب عند المصدر، وأُضيف `src/utils/json_safe.py` (`json_numpy_default`)
كطبقة حماية إضافية في كل موضع `json.dumps()` آخر يُسلسِل نتائج تحليل حقيقية (`video_analysis_page.py`,
`my_videos_page.py`, `history_manager.py`) — حتى أي تسرب numpy مستقبلي يتحول لنص بدل انهيار الصفحة.

**مهمة مؤجَّلة عمداً:** كان أحد الخيارات المطروحة "بناء كاشفات آلية لعناصر ISI التي تتطلب مراجعة
يدوية" (Arabesque، Shoot-the-Duck، إلخ) — بيانات نقاط الجسم (pose keypoints) تكفي نظرياً لقياس
بعضها (ساق ممدودة بارتفاع الورك، انثناء ركبة عميق)، لكن هذا المشروع أثبت مراراً (§4.6، §4.11) أن
أي كاشف حركة جديد **يجب التحقق منه على فيديو حقيقي قبل اعتماده** — ولا تتوفر حالياً فيديوهات
مرجعية كافية للتحقق. تم تأجيله عمداً بدل شحن كاشف غير مُتحقَّق منه قد يُنتج قرار نجاح/رسوب خاطئ
لاعب حقيقي. الأولوية: يرفع المستخدم فيديوهات مرجعية إضافية (خصوصاً لعناصر ISI الأساسية)، عندها
يُبنى الكاشف ويُتحقَّق منه بنفس منهجية jump/spin detection الصارمة.

---

## 5. قرارات تصميمية مهمة (لماذا تم الاختيار هكذا)

1. **الصدق قبل الثقة الزائفة** — مبدأ متكرر في كل قرار: لا نُخمِّن نتيجة قطعية عندما لا تتوفر
   بيانات كافية. أمثلة: لوحة حدود اللقطة، حالة `manual` في `evaluate()`، تحذير عند استخدام
   الطريقة الاحتياطية بدل MediaPipe.
2. **العتبات التكيّفية أفضل من الثابتة** — أي عتبة تعتمد على مسافة الكاميرا أو حجم اللاعب في
   الإطار يجب أن تتكيّف مع بيانات الفيديو نفسه (`global_x_std`)، وليس رقماً مطلقاً واحداً يناسب
   كل الحالات.
3. **لا نبني ميزة نصف مكتملة** — عندما لا يوجد كاشف آلي لعنصر (Arabesque، Three-Turns)، النظام
   يقول ذلك صراحة ويطلب من المدرب التقييم اليدوي، بدل تزييف نتيجة.
4. **`YASOOO.bat` هو نقطة الدخول الوحيدة المعتمدة على ويندوز** — كل الملفات القديمة الأخرى
   (`START.bat`, `run.bat`, `QUICK_START.bat`) صُحِّحت لتشير لـ `final_app.py` كذلك، لكن
   `YASOOO.bat` هو المرجع الأساسي دائماً.
5. **`YASOOO.bat` يجب أن يبقى ASCII بحتاً — بلا استثناء.** لا نص عربي، لا شرطات طويلة (—)، لا أي
   حرف Unicode، حتى مع `chcp 65001`. جرّبنا هذا وفشل بصمت على جهاز مستخدم حقيقي (راجع §4.12-د) —
   `cmd.exe` غير موثوق بما يكفي عبر كل إصدارات/لغات ويندوز لتحمّل الترميز المختلط في ملف حرج
   كهذا. أي رسالة عربية مطلوبة تذهب للتطبيق نفسه (Streamlit، الذي يتعامل مع Unicode بلا مشاكل)،
   وليس لملفات `.bat`/`.sh` التي تُشغّله.
6. **أي قيمة numpy تخرج من دالة قد تُحفظ لاحقاً كـ JSON يجب أن تُحوَّل صراحة بـ `float(...)` أو
   `bool(...)` عند نقطة الخروج.** `round()` والعمليات الحسابية على عنصر مصفوفة numpy تبقى
   numpy-typed حتى لو بدت رقماً عادياً — هذا تسبب بانهيار صفحة كاملة في الإنتاج (§4.12-هـ).
   `src/utils/json_safe.py` (`json_numpy_default`) طبقة حماية إضافية، وليست بديلاً عن الإصلاح
   عند المصدر.

---

## 6. كيفية المتابعة (للمساعد القادم)

1. اقرأ هذا الملف أولاً، ثم `CLAUDE.md` (تعليمات عامة للمستودع).
2. افحص `git log --oneline -30` على فرع `claude/continue-work-KRPKT` للتأكد من آخر حالة فعلية
   (هذا الملف قد يتأخر قليلاً عن أحدث commit).
3. أي فيديوهات ISI/ISU مرجعية جديدة يرفعها المستخدم → حللها بنفس الطريقة (استخراج إطارات كل 6-8
   ثوانٍ، كشف بطاقات العنوان الزرقاء عبر عدّ البكسلات الزرقاء، قراءة النص) → أضفها كمعيار جديد في
   `test_standards.py` باتباع نمط `ISI_DELTA` / `ISU_JUMP_BASICS` الموجود.
4. أي تعديل على منطق الكشف (`_detect_jumps`, `_detect_spins`, `_detect_errors`,
   `_detect_ai_errors`) **يجب اختباره بفيديو حقيقي** قبل الاعتماد عليه — الإصلاحات النظرية وحدها
   أثبتت أنها غير كافية عدة مرات في هذا المشروع (راجع §4.6 و§4.11). إن جرّبت إصلاحاً وأظهر
   نتائج أسوأ أو غير معقولة على بيانات حقيقية (كما حدث مع محاولة استخدام عمق Z للانحناء)، وثّق
   المحاولة المرفوضة بدل حذفها بصمت — تمنع تكرار نفس الخطأ لاحقاً.
5. لا تُضِف تبعيات/أطر عمل غير متوافقة مع طبيعة Python/Streamlit للمشروع دون تأكيد صريح من
   المستخدم.

---

*هذا الملف يُحدَّث يدوياً عند الحاجة — وليس تلقائياً. إذا طال الوقت منذ آخر تحديث، اعتبره نقطة
بداية تقريبية وتحقق من `git log` للتفاصيل الدقيقة.*
