# 🎿 دليل تشغيل نظام تحليل التزلج على Windows

## 📋 المتطلبات الأساسية

### 1. تثبيت Python
1. حمّل Python من الموقع الرسمي: https://www.python.org/downloads/
2. اختر آخر إصدار (Python 3.10 أو أحدث)
3. **مهم جداً**: أثناء التثبيت، فعّل خيار "Add Python to PATH"
4. اضغط "Install Now"
5. بعد انتهاء التثبيت، افتح Command Prompt واكتب:
   ```cmd
   python --version
   ```
   يجب أن يظهر رقم الإصدار (مثال: Python 3.11.0)

### 2. تحميل المشروع

#### الطريقة الأولى: باستخدام Git
```cmd
git clone https://github.com/elmansoory/yasooo.git
cd yasooo
git checkout claude/skating-analysis-system-019JhsA9HHwcSgQ6oXnujCjf
```

#### الطريقة الثانية: تحميل مباشر
1. اذهب إلى: https://github.com/elmansoory/yasooo
2. اضغط على "Code" ثم "Download ZIP"
3. فك ضغط الملف في المكان المناسب
4. افتح Command Prompt في مجلد المشروع

## 🚀 خطوات التشغيل

### الخطوة 1: تثبيت المكتبات المطلوبة

افتح Command Prompt في مجلد المشروع واكتب:

```cmd
pip install -r requirements.txt
```

سيتم تثبيت المكتبات التالية:
- streamlit (واجهة البرنامج)
- pandas (معالجة البيانات)
- plotly (الرسوم البيانية التفاعلية)
- opencv-python (معالجة الفيديو)
- ultralytics (ذكاء اصطناعي)
- pillow (معالجة الصور)

**ملاحظة**: التثبيت قد يأخذ بعض الوقت (5-10 دقائق)

### الخطوة 2: تشغيل البرنامج

```cmd
streamlit run app.py
```

ستظهر رسالة مثل:
```
  You can now view your Streamlit app in your browser.

  Local URL: http://localhost:8501
  Network URL: http://192.168.1.10:8501
```

### الخطوة 3: فتح البرنامج

- سيفتح البرنامج تلقائياً في المتصفح
- إذا لم يفتح، افتح المتصفح يدوياً واذهب إلى: http://localhost:8501

## 📊 استخدام البرنامج

### القسم الأول: لوحة التحكم الرئيسية
- **إحصائيات عامة**: عدد الأعضاء، متوسط الحضور، أيام التدريب
- **رسم بياني للحضور**: يوضح تطور الحضور عبر الزمن
- **توزيع حسب الجنس**: رسم دائري يوضح نسبة الذكور والإناث
- **أعلى 10 أعضاء حضوراً**: جدول تفاعلي

### القسم الثاني: ملفات الأعضاء
1. اختر عضو من القائمة المنسدلة
2. ستظهر معلومات تفصيلية:
   - معلومات شخصية (الاسم، الجنس، تاريخ الميلاد، العمر)
   - إحصائيات الحضور (عدد الحضور، آخر حضور، نسبة الحضور)
   - رسم بياني شهري للحضور
   - سجل الحضور الكامل

### القسم الثالث: تحليل الفيديو (وضع التجريبي)
- حالياً في الوضع التجريبي
- يعرض نتائج توضيحية
- يمكن رفع فيديو لكن سيعرض بيانات نموذجية

## 🔧 حل المشاكل الشائعة

### مشكلة: Missing optional dependency 'openpyxl'
**الحل**:
```cmd
pip install openpyxl
```
أو:
```cmd
python -m pip install openpyxl
```
**السبب**: هذه المكتبة ضرورية لقراءة ملفات Excel. بدونها لن يتم استيراد البيانات.

### مشكلة: Python is not recognized
**الحل**:
1. أعد تثبيت Python وتأكد من تفعيل "Add Python to PATH"
2. أو أضف Python يدوياً للـ PATH:
   - ابحث عن "Environment Variables" في Windows
   - أضف مسار Python (مثال: `C:\Python311\` و `C:\Python311\Scripts\`)

### مشكلة: pip is not recognized
**الحل**:
```cmd
python -m pip install -r requirements.txt
```

### مشكلة: خطأ أثناء تثبيت opencv-python
**الحل**:
```cmd
pip install opencv-python-headless
```
ثم عدّل `requirements.txt` واستبدل `opencv-python` بـ `opencv-python-headless`

### مشكلة: البرنامج بطيء
**الحل**:
- أغلق البرامج الأخرى
- تأكد من وجود 4GB RAM على الأقل متاحة
- استخدم متصفح Chrome أو Edge

### مشكلة: الرسوم البيانية لا تظهر
**الحل**:
1. تأكد من اتصال الإنترنت (Plotly يحتاج اتصال للمرة الأولى)
2. امسح cache المتصفح
3. جرب متصفح آخر

## 📁 هيكل الملفات

```
yasooo/
├── app.py                      # البرنامج الرئيسي
├── requirements.txt            # المكتبات المطلوبة
├── skating_database.db         # قاعدة البيانات
├── import_members.py           # برنامج استيراد الأعضاء
├── import_attendance.py        # برنامج استيراد الحضور
├── members.csv                 # ملف الأعضاء
├── attendance.csv              # ملف الحضور
└── WINDOWS_SETUP.md           # هذا الملف
```

## 🎯 نصائح للاستخدام الأمثل

1. **استخدم شاشة عريضة**: البرنامج مصمم للشاشات الكبيرة
2. **جرب الرسوم التفاعلية**: يمكنك التقريب والتحريك والتصدير
3. **استخدم البحث**: في الجداول يمكنك البحث بسهولة
4. **احفظ النتائج**: يمكنك تصدير الرسوم كصور PNG

## 📞 الدعم الفني

إذا واجهت أي مشكلة:
1. تحقق من قسم "حل المشاكل الشائعة" أعلاه
2. تأكد من تثبيت جميع المكتبات بشكل صحيح
3. تحقق من أن ملف `skating_database.db` موجود
4. جرب إعادة تشغيل البرنامج

## 🔄 تحديث البيانات

### لإضافة أعضاء جدد:
1. عدّل ملف `members.csv`
2. شغّل:
   ```cmd
   python import_members.py
   ```

### لإضافة سجلات حضور جديدة:
1. عدّل ملف `attendance.csv`
2. شغّل:
   ```cmd
   python import_attendance.py
   ```

### لعرض البيانات المحدثة:
1. أوقف البرنامج (Ctrl+C في Command Prompt)
2. شغّله مرة أخرى:
   ```cmd
   streamlit run app.py
   ```

## 🎨 تخصيص البرنامج

البرنامج يستخدم ألوان احترافية:
- **الأساسي**: #1f77b4 (أزرق)
- **الثانوي**: #ff7f0e (برتقالي)
- **النجاح**: #2ca02c (أخضر)

يمكنك تعديل الألوان في ملف `app.py` في قسم CSS.

---

**تم التطوير بواسطة**: Claude AI
**التاريخ**: مارس 2026
**الإصدار**: 1.0.0

🎿 استمتع باستخدام نظام تحليل التزلج! 🎿
